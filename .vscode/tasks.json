{
    // See https://go.microsoft.com/fwlink/?LinkId=733558
    // for the documentation about the tasks.json format
    "version": "2.0.0",
    "tasks": [
        {
            "label": "Build image with docker",
            "type": "shell",
            "command": "podman build --pull -f ./Containerfile -t localhost:5000/redtide:latest .",
            "group": {
                "kind": "build",
                "isDefault": true
            }
        }
    ]
}